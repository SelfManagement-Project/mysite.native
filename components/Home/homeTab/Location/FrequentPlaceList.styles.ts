import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    marginBottom: 16,
  },
  title: {
    fontWeight: 'bold',
    marginBottom: 8,
  },
  itemText: {
    paddingVertical: 4,
  },
  emptyText: {
    color: '#888',
  },
});

export default styles;
