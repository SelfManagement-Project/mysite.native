import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    marginBottom: 16,
  },
  title: {
    fontWeight: 'bold',
    marginBottom: 8,
  },
  emptyText: {
    color: '#888',
  },
  itemText: {
    marginBottom: 4,
  },
});

export default styles;
